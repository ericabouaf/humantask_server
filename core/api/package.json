{
   "name": "api",
   "main": "./api.js",

   "dependencies": {
      "node-uuid": "1.4.1"
   },

   "plugin": {
      "consumes": [ "httpserver", "eventbus", "logger" ],
      "provides": [ "api" ]
   }
}
